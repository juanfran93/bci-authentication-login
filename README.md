# bci-authentication-login

## Instrucciones de buildeo
Ejecutar por command line *gradlew build* sobre la carpeta root del proyecto

Ejecutar sobre un IDE (eclipse por ejemplo) como una Instancia Java Application

